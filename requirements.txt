# Core scientific stack
numpy
pandas
scipy
joblib

# Machine learning
scikit-learn
xgboost

# Deep learning (gaze tracking - AFFNet)
torch
torchvision

# Computer vision & face landmarks
opencv-python
# Pin to <0.10.20 — newer versions remove the legacy `mp.solutions` namespace
# on macOS arm64. See engine/pyproject.toml for details.
mediapipe>=0.10,<0.10.20
Pillow

# UI / experiment runtime
pygame
